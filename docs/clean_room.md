# Clean-Room Notes

This repository is an original implementation.

Constraints used while building it:

- no source files copied from the upstream repository
- no relicensing of upstream code
- only high-level functional requirements were used
- naming, layout, and module boundaries were recreated from scratch

The upstream project remains a reference for the problem domain, not the implementation.
