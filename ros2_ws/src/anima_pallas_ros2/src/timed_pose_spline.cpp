#include <anima_pallas_ros2/timed_pose_spline.hpp>

// Intentionally left as a translation unit anchor.
// The spline implementation lives inline in the header so it remains usable
// without requiring CMake changes in this pass.

